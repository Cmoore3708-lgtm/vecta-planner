# v41.1n - Planner Card Stack Fix

- Keeps the registration and time sizing.
- Forces the booking status directly underneath the time.
- Forces the ramp badge directly underneath the status.
- Applies the fix to both the root static `index.html` and `dist/index.html`.


## v41.1r - Job Template Reordering
- Added drag-and-drop ordering for preset job templates in Settings.
- Added up/down buttons for easier reordering on laptops and phones.
- Saved template order is used by the Add Job template picker.
- Excludes node_modules from the deployable ZIP.
