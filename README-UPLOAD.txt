Replace these three existing files in the repository, preserving their paths:

booking.html
public/booking.html
dist/booking.html

This fixes the registration input losing focus after every character.
