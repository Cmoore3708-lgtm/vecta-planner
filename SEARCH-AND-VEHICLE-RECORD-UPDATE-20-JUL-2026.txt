VECTA Workshop Pro - Search and Vehicle Record update

- Removed the separate Customers navigation tab.
- Renamed Vehicles to Search.
- Removed the + Vehicle button from the search workflow.
- Added Vehicle registration and Customer details search modes.
- Fixed filtered search results so they remain clickable.
- Fixed the search typing crash caused by the input handler retaining a shared
  variable that was later overwritten with null.
- Prevented the three-second cloud synchronisation refresh from replacing the
  active search box and clearing the search text.
- Search text is now preserved across later page refreshes.
- Redesigned Print Day Plan as clear mechanic columns without the redundant
  time rail.
- Every printed job now has a readable minimum-height card showing registration,
  vehicle, work required, booking time, ramp and a large job type.
- Removed job status from printed planner cards to release space for useful work
  information.
- Rebuilt the planner control rail as two side-by-side columns for My Tasks and
  Unallocated Jobs on desktop screens.
- Preserved the task and unallocated-list scroll positions during live Supabase
  refreshes, preventing the lists from snapping back to the top.
- Condensed task cards, moved Edit/Delete controls to the top-right and reduced
  unallocated job-card height.
- Reduced the desktop navigation width to release more room for the planner and
  control rail.
- Reduced the combined planner control rail from 500px to 380px to restore the
  main mechanic columns.
- Reordered the control rail to show Unallocated Jobs on the left and My Tasks
  on the right.
- Removed the separate task Edit button; clicking a task card now opens it while
  checkbox, pin and Delete remain independent controls.
- Added vehicles found in job history even when a separate vehicle row is missing.
- Registration clicks now open a read-only Vehicle Record.
- Added vehicle information, MOT/tax status, customer details and chronological history.
- Added direct actions for Edit Vehicle, Create New Job, Print Vehicle History,
  View Service Sheet and View Invoice (when records exist).
- Job cards, service sheets/health checks and invoices open directly from history.

Validation:
- Embedded application JavaScript syntax check passed.
- Production dependency installation was attempted but the package registry returned
  a temporary 502 error. The application itself is a static Vercel build and the
  updated index.html passed its direct JavaScript parse check.
